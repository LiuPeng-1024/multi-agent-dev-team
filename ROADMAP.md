# ROADMAP

已知缺口与演进方向。每条都标注了"为什么重要"与"验收长什么样"。欢迎认领（开 Issue 讨论后 PR）。

## P0 — 协议化：从"进程内团队"到"可互联团队"

对标 HTTP 的成功要素（统一接口/元数据载荷分离/错误语义/版本演进/传输无关）与 A2A/MCP 的现状，本框架目前只做了"语义层"，缺以下三层：

### 1. 能力发现层（Agent Card）
- **现状**：角色与模型席位写死在文档里（`llm_proxy.yaml` 手写席位表 = 人肉 Agent Card）。
- **目标**：每个角色/模型席位发布机器可读的能力卡片：擅长任务类型、接受输入格式、SLA（时长/token/费用）、已验证的探测数据（参考 `multi-model-review/examples/immune_memory.example.md` §模型×任务适配档案）。
- **验收**：编排者能在运行时查询卡片做席位路由，而不是读散文。

### 2. 认知预算条款（跨模型协作的"账单协议"）
- **现状**：消息里不带预期/实际消耗。已实测的坑：思考型模型 reasoning_tokens 吃满 max_tokens 配额导致空回复；同任务不同模型 token 消耗差 1.9 倍（账单几乎相同——定价便宜被消耗量抵消）。
- **目标**：每次跨角色/跨模型调用声明预算上限并回报实际消耗；超预算触发画像降级（接 `exception-handling.md` 的 P1-P4）。
- **验收**：一次完整外层循环能产出逐席位 token/费用报表。

### 3. 传输层（路由与传输分离）
- **现状**：消息路由语义已存在（from/to/cause-by 三元组 + 按类型订阅，见 MetaGPT 式发布-订阅在本框架的对应物：区块文档 + 角色 watch），但传输是"同一个会话/进程"。
- **目标**：消息信封可跨进程/跨 harness 传输（候选：A2A JSON-RPC over HTTP(S) + SSE 长任务流式）。
- **验收**：一个角色跑在 Kimi CLI、另一个跑在 ZCode，经标准信封协作完成一轮内层循环。

## P1 — 信任层

- **消息签名与来源验证**：跨组织协作时防伪造/防注入（当前框架只有会话内信任）。
- **审计追溯标准化**：幻觉日志（区块 11）目前是 Markdown 表格，升级为结构化可查询格式，支持跨项目聚合"模型×任务错配"数据。

## P1 — 审查层工程化

- `review_engine` 补测试套件（当前是生产在用但无单测——先还自己的债）。
- 仲裁模型的探测数据复测流程化：`check_health` + 真实仲裁抽样，结果自动回填免疫记忆档案。
- 支持更多异构审查对（不限于 kimi+deepseek），审查对选择由能力卡片驱动（联动 P0-1）。

## P2 — 内容与生态

- `examples/` 实战范例库：脱敏的完整 `PROJECT_CONTEXT.md` 填写实例（回合制游戏/Tauri 桌面工具/数据管线）。
- 专家注入包许可梳理：为 `references/skills/` 建立明确许可的自有专家包（替代当前不分发的第三方包）。
- 文档英文化（当前中文优先）。

## 明确不做

- 不造大一统编排引擎：本框架的价值在"harness 无关的文档+门禁"，编译成代码管线会丢掉这个特性（那会变成另一个 MetaGPT）。
- 不在协议成熟前引入跨进程依赖：单会话内的结构保证已经可度量（保证度三因子），先保住在手能力。
