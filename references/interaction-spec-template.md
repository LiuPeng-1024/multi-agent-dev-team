# 界面交互规约模板 (INTERACTION_SPEC.md)

> 01 + 03 co-author。实现级规约——讲"每个交互在每种状态下怎么表现"。
> **独立成文** `docs/INTERACTION_SPEC.md`，不进 SSOT（变更频率高于 PRD、低于代码）。
> **freeze 才准开工**：未经 06 对抗性审 + 人 gate freeze，dev 不许动 UI（防 dev 期间规约漂移）。
> **区块3 P0 的 `交互规约章节` 字段**指向本文具体 §；guard-check 检查7 弱 oracle 卡空值。
> 落地流程见 `collaboration-flow.md` 阶段0.0 规格化生产。

**分工**：
- 01 主笔：§1 状态机的产品侧（有哪些状态/每态显示什么）、§2 组件契约的产品侧（有哪些交互）、§4 边界异常的业务侧、§5 a11y
- 03 主笔：**§3 状态/数据契约**（state shape/谁写谁读/生命周期/切换时旧数据去留）、§1 状态机的转换侧、§2 的数据依赖
- 01 起草 §6 回归断言(从 §1-§5 派生可断言验收) → 05 测试工程师 dev 期转可执行 UI 自动化(喂 V-UI-STRUCT) → 06 审查 §6 可执行性(freeze 前,审查重点第8条)

---

## §0 元信息

| 字段 | 值 |
|------|-----|
| 版本 | [v0.1] |
| co-author | 01 / 03 |
| 日期 | [YYYY-MM-DD] |
| 状态 | [草拟 / 06审查中 / 已freeze / 变更中] |
| 配套 PRD | `docs/prd.md` |
| 配套 DESIGN.md | `docs/DESIGN.md`(design 主笔,审美方向;交互规约不抢 §3 仍归 03) |

---

## §1 界面状态机

> 每个屏/视图一个状态机。列出所有状态 + 转换 + 每态显示什么。
> 01 写状态与每态显示；03 写转换条件与数据触发。

### §1.x [视图名]

**状态**：
| 状态 | 显示 | 进入条件 |
|------|------|---------|
| idle | [显示什么] | [初始/进入条件] |
| loading | [骨架屏/旧数据+spinner] | [触发] |
| loaded | [内容] | [数据到位] |
| empty | ["无合格产品"] | [loaded 但数据空] |
| error | ["错误: {e}" + 重试] | [请求失败] |
| retry | [重试中] | [用户点重试] |

**转换图**（文字描述即可）：
```
idle --(用户切场景)--> loading --(成功)--> loaded/empty --(切场景)--> loading
                                     \--(失败)--> error --(点重试)--> retry --> loading
```

---

## §2 组件交互契约

> 每个交互组件一条。动画给**精确值**（duration/easing/stagger），不写"淡入"散文。

### §2.x [组件名]
- **触发**：click / hover / focus / 键盘(Enter/Space/Tab/Esc)
- **预期行为**：[点击后发生什么]
- **状态变化**：[哪个 state 变 / 副作用]
- **动画**：[属性 from→to, duration Xms, easing, stagger Xms]（精确值）
- **数据依赖**：[读哪个 state / 调哪个 invoke]（03 填）

---

## §3 状态/数据契约  ← 03 主笔

> 共享 state 的 shape、谁写谁读、生命周期、切换时旧数据去留。
> 这是交互规约的架构侧；产品侧（§1/§2 的"有哪些")归 01。

### §3.x [共享状态名]
- **shape**：`{ currentScene: string, rankData: RankedProduct[], loading: bool, error: string|null }`
- **写者**：[哪个组件/回调写]
- **读者**：[哪些组件读]
- **生命周期**：[何时创建/销毁]
- **切换时旧数据去留**：[保留显示直到新数据到 / 立即清空显骨架 / ...]
- **错误约定**：[错误 state shape + 谁恢复]

---

## §4 边界与异常交互

> 不只测正常路径。每类异常的交互表现。

| 异常 | 交互表现 | 恢复 |
|------|---------|------|
| invoke 失败 | [显示什么] | [自动重试/人重试] |
| 空数据 | [显示什么] | — |
| 快速连切（防抖） | [debounce Xms / 取消上次请求] | [只渲染最后一次] |
| 慢网络 | [骨架屏 vs 旧数据] | [超时阈值] |
| 并发请求竞态 | [取消旧请求/忽略旧响应] | — |

---

## §5 行为级 a11y

> impeccable 审计查静态 a11y（对比度）；这里查**行为级**。

- **tab 序**：[焦点遍历顺序]
- **焦点管理**：[场景切换后焦点去哪 / 弹层焦点陷阱]
- **ARIA live**：[loading/error/结果区是 aria-live="polite"/"assertive"]
- **键盘等价**：[每个鼠标交互的键盘等价]

---

## §6 回归断言 → 喂 V-UI-STRUCT

> 每条 → 一条可执行 UI 自动化断言（Playwright / 移动端 UI 自动化 / 截图 diff，按项目技术栈）。
> 这是 V-UI-STRUCT★ 的 oracle 来源——把"交互回归"从判断类（无 oracle）升级为可执行。

| 断言ID | 断什么 | 技术栈/命令 | 期望 |
|--------|--------|-----------|------|
| A-01 | 切换场景触发重新拉取 | [playwright/移动端命令] | pass |
| A-02 | 空数据显示"无合格产品" | [...] | pass |
| A-03 | invoke 失败显示错误+重试 | [...] | pass |
| A-04 | 快速连切只渲染最后一次 | [...] | pass |
| A-05 | loading 态显示骨架 | [...] | pass |

> 项目据此填区块8.3 的 V-UI-STRUCT★ 行（命令列填本表技术栈命令，期望 pass）。
